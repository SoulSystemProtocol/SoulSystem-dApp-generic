/**
 * Page for a DAO detail
 */
import * as React from 'react';
import { Box } from '@mui/material';

interface DaoProps {}

export default function DaoDetailPage({}: DaoProps) {
  return <Box>Dao Detail</Box>;
}
